const express=require('express'),port=3e3;var router=express.Router(),cors=require('cors');let sentiment=require('./Sentiment');const app=express();app.use(cors()),app.get('/sentimentData',function(a,b){sentiment.getCoinScore(a.query.name).then(d=>{b.status(200).send(d.toString())},function(){console.log('error')})}),app.listen(port,function(){console.log('Server is running on '+port+' port')});